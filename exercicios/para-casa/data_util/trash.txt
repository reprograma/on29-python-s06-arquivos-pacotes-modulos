def calcular_idade(data_nascimento):
        hoje = date.today()
        datetime.datetime(year=year, month=month, day=day)
        nasc = datetime.strptime(data_nascimento, "%d/%m/%Y")
        idade_ano = hoje.year - nasc.year
        idade_mes = hoje.month - nasc.month
        idade_dia = hoje.day - nasc.day
        print(f"Sua idade é {idade_ano} anos, {idade_mes} meses e {idade_dia} dias.")

def validar_data():
    hoje = date.today()
    nasc = datetime.strptime(data_nascimento, "%d/%m/%Y")
    dia = nasc.day
    mes = nasc.month
    ano = nasc.year

    meses30 = [4, 6, 9, 11]
    meses31 = [1, 3, 5, 7, 8, 10, 12]

    if mes in meses30 and (dia <= 0 or dia > 30):
        print("Dia inválido. Informe sua data de nascimento no formato (DD/MM/AAAA)")
        return
    elif mes in meses31 and (dia <= 0 or dia > 31):
        print("Dia inválido. Informe sua data de nascimento no formato (DD/MM/AAAA)")
        return
    elif mes == 2:
        if ano % 4 == 0 and (ano % 100 != 0 or ano % 400 == 0):
            if dia > 29:
                print("Dia inválido. Informe sua data de nascimento no formato (DD/MM/AAAA)")
                return
        elif dia > 28:
            print("Dia inválido. Informe sua data de nascimento no formato (DD/MM/AAAA)")
            return
